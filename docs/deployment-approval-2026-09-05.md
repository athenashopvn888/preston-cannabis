# Preston deployment approval continuity

The owner explicitly answered “yes do it” to deploying Preston into the fleet Vercel team `athenashopvn888-2439s-projects` and connecting `prestoncannabis.com` plus `www.prestoncannabis.com`, with noindex retained. This is a new approval extending the earlier local/preview-only scope; earlier onboarding records remain historical.

## Exact authorized scope

- Executor Agent X; new `preston-cannabis` project only.
- Vercel team `athenashopvn888-2439s-projects`, organization `team_IqYig3d4WKblBzRjJ50L7GEc`.
- Authenticate in an isolated `.vercel-auth-preston` directory. Do not change the existing global CLI account.
- Upload the verified latest Preston app, preserving the accepted hero/Exotic images and completed graphics revision.
- Configure only the approved temporary CHC01 source endpoint as a server-side encrypted variable in the new project. Never print it or copy unrelated source environment values.
- Validate a candidate deployment before assigning the two approved domains. Keep all noindex controls, temporary-source notices and source identity checks.
- Parent handles registrar DNS UI; change only the web-routing records requested by Vercel and preserve existing BasicDNS, MX and mail/TXT records.

No other project/domain, existing store, GBP, GSC, FMD, registry, Google Sheet or Apps Script may be changed. Canonical POD9 registration, an official Preston store code, Preston-specific inventory and indexed-launch approval remain pending.

Authentication, deployment identifiers, exact DNS requirements and independent live-readback results will be recorded separately after they are verified. Approval does not itself prove deployment success.
